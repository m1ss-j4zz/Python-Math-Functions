[project]
name = "Python Math Functions"
version = "0.1.0"
description = "Math Functions Not In Base Python"
authors = [{ name = "Jaz"}]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"